{
  "name": "agent-kernel-workspace",
  "private": true,
  "workspaces": [
    "packages/typescript",
    "packages/inspector"
  ],
  "scripts": {
    "typecheck:contracts": "npm run typecheck --workspace=@agent-kernel/contracts-client",
    "conformance:contracts": "npm run conformance --workspace=@agent-kernel/contracts-client",
    "typecheck:inspector": "npm run typecheck --workspace=@agent-kernel/responsibility-inspector",
    "build:inspector": "npm run build --workspace=@agent-kernel/responsibility-inspector"
  }
}
