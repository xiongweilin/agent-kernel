name: B4 P1 patch

on:
  push:
    branches: [feat/recovery-observation-authority-impl]
    paths: [.github/workflows/_b4_p1_patch.yml]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: feat/recovery-observation-authority-impl
      - name: Apply B4 P1 semantic patch
        run: |
          python - <<'PY'
          from pathlib import Path

          def patch(path: str, old: str, new: str) -> None:
              p = Path(path)
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f"anchor not found: {path}: {old[:80]!r}")
              p.write_text(text.replace(old, new, 1))

          recovery_module = '''"""Durable, execution-level observations for ambiguous committed dispatches.

          RecoveryObservation is deliberately not objective Outcome authority and not a
          recovery decision.  The store re-derives its execution binding from the exact
          InvocationDispatchCommitted graph before one observation instance can be
          durably recorded.
          """

          from __future__ import annotations

          import hashlib
          import json
          from dataclasses import dataclass
          from typing import Any, Literal, Protocol, cast

          from portable_runtime.core.models import Action, Event, Step, StepAttempt

          RECOVERY_OBSERVATION_EVENT = "RecoveryObservationRecorded"
          RECOVERY_OBSERVATION_SCHEMA = "recovery-observation-v1"
          DISPATCH_COMMIT_EVENT = "InvocationDispatchCommitted"
          DISPATCH_COMMIT_SCHEMA = "governance-dispatch-commit-v1"

          RecoveryReportedStatus = Literal[
              "reported-succeeded",
              "reported-failed",
              "reported-unknown",
          ]
          _REPORTED_STATUSES = frozenset(
              {"reported-succeeded", "reported-failed", "reported-unknown"}
          )


          @dataclass(frozen=True)
          class RecoveryObservationCommitRequest:
              """Caller claim identifying one observation instance of one dispatch."""

              observation_instance_ref: str
              dispatch_commit_ref: str
              observation_source: str
              reported_status: RecoveryReportedStatus
              provenance_refs: tuple[str, ...] = ()


          @dataclass(frozen=True)
          class RecoveryObservation:
              """Durable execution-level fact; never objective or recovery authority."""

              id: str
              observation_instance_ref: str
              dispatch_commit_ref: str
              action_ref: str
              attempt_ref: str
              step_ref: str
              request_ref: str
              provider_id: str
              idempotency_key: str | None
              observation_source: str
              reported_status: RecoveryReportedStatus
              provenance_refs: tuple[str, ...]
              durable: bool = True
              authoritative_outcome: bool = False


          @dataclass(frozen=True)
          class PreparedRecoveryObservationCommit:
              observation: RecoveryObservation
              event: Event


          class RecoveryObservationStoreReader(Protocol):
              def get_event(self, event_id: str) -> Event | None: ...
              def get_attempt(self, attempt_id: str) -> StepAttempt | None: ...
              def get_step(self, step_id: str) -> Step | None: ...
              def get_action(self, action_id: str) -> Action | None: ...


          def _canonical(value: object) -> str:
              return json.dumps(value, ensure_ascii=False, sort_keys=True, separators=(",", ":"))


          def _digest(value: object) -> str:
              return hashlib.sha256(_canonical(value).encode()).hexdigest()


          def _required_string(payload: dict[str, Any], key: str) -> str:
              value = payload.get(key)
              if not isinstance(value, str) or not value.strip():
                  raise ValueError(f"recovery observation requires dispatch {key}")
              return value


          def _dispatch_commit_ref(payload: dict[str, Any]) -> str:
              identity = {
                  "schema": payload.get("schema"),
                  "request_id": payload.get("request_id"),
                  "provider_id": payload.get("provider_id"),
                  "attempt_id": payload.get("attempt_ref"),
                  "invocation_permit_digest": payload.get("invocation_permit_digest"),
                  "governance_requirement_digest": payload.get("governance_requirement_digest"),
                  "governance_snapshot_digest": payload.get("governance_snapshot_digest"),
              }
              return f"dispatch_{_digest(identity)}"


          def reported_status_from_capability_status(status: str) -> RecoveryReportedStatus:
              if status == "succeeded":
                  return "reported-succeeded"
              if status == "failed":
                  return "reported-failed"
              return "reported-unknown"


          def _semantic_payload(value: object) -> dict[str, object]:
              dumper = getattr(value, "model_dump", None)
              if not callable(dumper):
                  raise ValueError("recovery observation replay requires typed event facts")
              payload = dict(dumper(mode="json"))
              payload.pop("created_at", None)
              return payload


          def same_recovery_observation_semantics(left: object, right: object) -> bool:
              return _semantic_payload(left) == _semantic_payload(right)


          def recovery_observation_from_event(event: Event) -> RecoveryObservation:
              if event.type != RECOVERY_OBSERVATION_EVENT:
                  raise ValueError("event is not a RecoveryObservationRecorded fact")
              payload = event.payload if isinstance(event.payload, dict) else {}
              if payload.get("schema") != RECOVERY_OBSERVATION_SCHEMA:
                  raise ValueError("unsupported recovery observation schema")
              if payload.get("semantic_level") != "recovery-observation":
                  raise ValueError("recovery observation semantic level mismatch")
              if payload.get("authoritative_outcome") is not False:
                  raise ValueError("RecoveryObservation cannot claim objective authority")
              reported_status = payload.get("reported_status")
              if reported_status not in _REPORTED_STATUSES:
                  raise ValueError("invalid recovery observation reported status")
              provenance_raw = payload.get("provenance_refs", [])
              if not isinstance(provenance_raw, list) or not all(
                  isinstance(ref, str) and ref for ref in provenance_raw
              ):
                  raise ValueError("invalid recovery observation provenance refs")
              idempotency_key = payload.get("idempotency_key")
              if idempotency_key is not None and not isinstance(idempotency_key, str):
                  raise ValueError("invalid recovery observation idempotency key")
              return RecoveryObservation(
                  id=event.id,
                  observation_instance_ref=_required_string(payload, "observation_instance_ref"),
                  dispatch_commit_ref=_required_string(payload, "dispatch_commit_ref"),
                  action_ref=_required_string(payload, "action_ref"),
                  attempt_ref=_required_string(payload, "attempt_ref"),
                  step_ref=_required_string(payload, "step_ref"),
                  request_ref=_required_string(payload, "request_ref"),
                  provider_id=_required_string(payload, "provider_id"),
                  idempotency_key=idempotency_key,
                  observation_source=_required_string(payload, "observation_source"),
                  reported_status=cast(RecoveryReportedStatus, reported_status),
                  provenance_refs=tuple(provenance_raw),
              )


          def prepare_recovery_observation_commit(
              store: RecoveryObservationStoreReader,
              request: RecoveryObservationCommitRequest,
          ) -> PreparedRecoveryObservationCommit:
              """Re-read a committed dispatch graph and build one deterministic observation."""

              instance_ref = request.observation_instance_ref.strip()
              dispatch_ref = request.dispatch_commit_ref.strip()
              source = request.observation_source.strip()
              if not instance_ref or not dispatch_ref or not source:
                  raise ValueError("recovery observation requires instance, dispatch, and source refs")
              if request.reported_status not in _REPORTED_STATUSES:
                  raise ValueError("invalid recovery observation reported status")
              provenance = tuple(sorted({ref.strip() for ref in request.provenance_refs if ref.strip()}))
              if len(provenance) != len(request.provenance_refs):
                  raise ValueError("recovery observation provenance refs must be non-empty and unique")

              dispatch = store.get_event(dispatch_ref)
              if dispatch is None or dispatch.type != DISPATCH_COMMIT_EVENT:
                  raise ValueError("recovery observation requires exact InvocationDispatchCommitted event")
              payload = dispatch.payload if isinstance(dispatch.payload, dict) else {}
              if payload.get("schema") != DISPATCH_COMMIT_SCHEMA:
                  raise ValueError("recovery observation dispatch schema mismatch")
              if payload.get("linearization_domain") != "authoritative-state-store":
                  raise ValueError("recovery observation dispatch linearization domain mismatch")
              if _dispatch_commit_ref(payload) != dispatch.id or dispatch.id != dispatch_ref:
                  raise ValueError("recovery observation dispatch deterministic identity mismatch")

              request_ref = _required_string(payload, "request_id")
              provider_id = _required_string(payload, "provider_id")
              attempt_ref = _required_string(payload, "attempt_ref")
              if dispatch.subject_ref != request_ref:
                  raise ValueError("recovery observation dispatch subject binding mismatch")
              attempt = store.get_attempt(attempt_ref)
              if attempt is None:
                  raise ValueError("recovery observation requires durable StepAttempt")
              metadata = attempt.metadata if isinstance(attempt.metadata, dict) else {}
              if metadata.get("dispatch_commit_ref") != dispatch_ref:
                  raise ValueError("recovery observation attempt/dispatch binding mismatch")
              if attempt.request_ref != request_ref or attempt.provider_id != provider_id:
                  raise ValueError("recovery observation attempt request/provider binding mismatch")
              for key in (
                  "invocation_permit_digest",
                  "governance_requirement_digest",
                  "governance_snapshot_digest",
              ):
                  if metadata.get(key) != payload.get(key):
                      raise ValueError(f"recovery observation dispatch {key} binding mismatch")
              if attempt.lease_generation != payload.get("lease_generation"):
                  raise ValueError("recovery observation lease generation binding mismatch")

              step = store.get_step(attempt.step_id)
              if step is None:
                  raise ValueError("recovery observation requires durable Step")
              action_ref = metadata.get("action_ref")
              if not isinstance(action_ref, str) or not action_ref:
                  raise ValueError("recovery observation requires exact Attempt-to-Action binding")
              action = store.get_action(action_ref)
              if action is None:
                  raise ValueError("recovery observation requires durable Action")
              if action.request_ref != request_ref or action.provider_id != provider_id:
                  raise ValueError("recovery observation Action request/provider binding mismatch")
              if action.run_id != step.run_id:
                  raise ValueError("recovery observation Action/Step run binding mismatch")

              observation_id = f"recovery_observation_{_digest({'schema': RECOVERY_OBSERVATION_SCHEMA, 'observation_instance_ref': instance_ref})[:32]}"
              event_payload = {
                  "schema": RECOVERY_OBSERVATION_SCHEMA,
                  "semantic_level": "recovery-observation",
                  "authoritative_outcome": False,
                  "observation_instance_ref": instance_ref,
                  "dispatch_commit_ref": dispatch_ref,
                  "action_ref": action.id,
                  "attempt_ref": attempt.id,
                  "step_ref": step.id,
                  "request_ref": request_ref,
                  "provider_id": provider_id,
                  "idempotency_key": attempt.idempotency_key,
                  "observation_source": source,
                  "reported_status": request.reported_status,
                  "provenance_refs": list(provenance),
              }
              event = Event(
                  id=observation_id,
                  type=RECOVERY_OBSERVATION_EVENT,
                  subject_ref=dispatch_ref,
                  payload=event_payload,
              )
              return PreparedRecoveryObservationCommit(
                  observation=recovery_observation_from_event(event),
                  event=event,
              )
          '''
          Path("src/portable_runtime/workflows/recovery_observation.py").write_text(recovery_module)

          patch(
              "src/portable_runtime/interfaces/store.py",
              "    def commit_verified_outcome(self, request: VerifiedOutcomeCommitRequest) -> OutcomeRecord: ...\n",
              "    def commit_verified_outcome(self, request: VerifiedOutcomeCommitRequest) -> OutcomeRecord: ...\n"
              "    def commit_recovery_observation(self, request: Any) -> Any: ...\n",
          )

          patch(
              "src/portable_runtime/core/boundary_stages.py",
              '''            action = Action(
                id=new_id("action"),
                work_id=request.work_id,
                run_id=request.run_id,
                capability=request.capability,
                provider_id=provider_id,
                request_ref=request.id,
                status="running",
            )
            if hasattr(store, "transaction"):
''',
              '''            action = Action(
                id=new_id("action"),
                work_id=request.work_id,
                run_id=request.run_id,
                capability=request.capability,
                provider_id=provider_id,
                request_ref=request.id,
                status="running",
            )
            attempt = attempt.model_copy(
                update={"metadata": {**attempt.metadata, "action_ref": action.id}}
            )
            if hasattr(store, "transaction"):
''',
          )

          patch(
              "src/portable_runtime/stores/memory.py",
              "        self._outcome_impact_commit_depth = 0\n",
              "        self._outcome_impact_commit_depth = 0\n        self._recovery_observation_commit_depth = 0\n",
          )
          patch(
              "src/portable_runtime/stores/memory.py",
              '''            return committed_outcome_impact(prepared)

    def _validate_candidate_write(self, kind: str, value: BaseModel) -> None:
''',
              '''            return committed_outcome_impact(prepared)

    def commit_recovery_observation(self, request: Any) -> Any:
        """Atomically commit or replay one execution-level recovery observation."""
        from portable_runtime.workflows.recovery_observation import (
            prepare_recovery_observation_commit,
            recovery_observation_from_event,
            same_recovery_observation_semantics,
        )

        with self.transaction():
            prepared = prepare_recovery_observation_commit(self, request)
            existing = self.get_event(prepared.event.id)
            if existing is not None:
                if not same_recovery_observation_semantics(existing, prepared.event):
                    raise ValueError("RecoveryObservation identity rebound")
                return recovery_observation_from_event(existing)
            self._recovery_observation_commit_depth += 1
            try:
                self.append_event(prepared.event)
            finally:
                self._recovery_observation_commit_depth -= 1
            return prepared.observation

    def _validate_candidate_write(self, kind: str, value: BaseModel) -> None:
''',
          )
          patch(
              "src/portable_runtime/stores/memory.py",
              '''        if value.type in OUTCOME_IMPACT_AUTHORITY_EVENT_TYPES and self._outcome_impact_commit_depth <= 0:
            raise ValueError("Outcome impact authority events require commit_outcome_impact_judgment")
        with self._lock:
''',
              '''        if value.type in OUTCOME_IMPACT_AUTHORITY_EVENT_TYPES and self._outcome_impact_commit_depth <= 0:
            raise ValueError("Outcome impact authority events require commit_outcome_impact_judgment")
        if value.type == "RecoveryObservationRecorded" and self._recovery_observation_commit_depth <= 0:
            raise ValueError("RecoveryObservation events require commit_recovery_observation")
        with self._lock:
''',
          )

          patch(
              "src/portable_runtime/stores/sqlite.py",
              "        self._outcome_impact_commit_depth = 0\n",
              "        self._outcome_impact_commit_depth = 0\n        self._recovery_observation_commit_depth = 0\n",
          )
          patch(
              "src/portable_runtime/stores/sqlite.py",
              '''                raise

    def _validate_candidate_write(self, kind: str, value: Any) -> None:
''',
              '''                raise

    def commit_recovery_observation(self, request: Any) -> Any:
        """Writer-serialized commit/replay of one durable recovery observation."""
        from portable_runtime.workflows.recovery_observation import (
            prepare_recovery_observation_commit,
            recovery_observation_from_event,
            same_recovery_observation_semantics,
        )

        with self._lock:
            owns_transaction = not self._connection.in_transaction
            if owns_transaction:
                self._connection.execute("BEGIN IMMEDIATE")
            try:
                prepared = prepare_recovery_observation_commit(self, request)
                existing = self.get_event(prepared.event.id)
                if existing is not None:
                    if not same_recovery_observation_semantics(existing, prepared.event):
                        raise ValueError("RecoveryObservation identity rebound")
                    observation = recovery_observation_from_event(existing)
                    if owns_transaction:
                        self._connection.execute("COMMIT")
                    return observation
                self._recovery_observation_commit_depth += 1
                try:
                    self.append_event(prepared.event)
                finally:
                    self._recovery_observation_commit_depth -= 1
                if owns_transaction:
                    self._connection.execute("COMMIT")
                return prepared.observation
            except Exception:
                if owns_transaction:
                    self._rollback(self._connection.cursor())
                raise

    def _validate_candidate_write(self, kind: str, value: Any) -> None:
''',
          )
          patch(
              "src/portable_runtime/stores/sqlite.py",
              '''        if value.type in OUTCOME_IMPACT_AUTHORITY_EVENT_TYPES and self._outcome_impact_commit_depth <= 0:
            raise ValueError("Outcome impact authority events require commit_outcome_impact_judgment")
        existing = self._get("event", Event, value.id)
''',
              '''        if value.type in OUTCOME_IMPACT_AUTHORITY_EVENT_TYPES and self._outcome_impact_commit_depth <= 0:
            raise ValueError("Outcome impact authority events require commit_outcome_impact_judgment")
        if value.type == "RecoveryObservationRecorded" and self._recovery_observation_commit_depth <= 0:
            raise ValueError("RecoveryObservation events require commit_recovery_observation")
        existing = self._get("event", Event, value.id)
''',
          )

          patch(
              "src/portable_runtime/core/runtime.py",
              '''        result = await self.capabilities.reconcile(last.request_ref, last.provider_id)
        if result:
            if result.status == "unknown":
                step.status = "unknown"
                self.store.save_step(step)  # type: ignore
            return result
''',
              '''        result = await self.capabilities.reconcile(last.request_ref, last.provider_id)
        if result:
            metadata = last.metadata if isinstance(last.metadata, dict) else {}
            dispatch_commit_ref = metadata.get("dispatch_commit_ref")
            if isinstance(dispatch_commit_ref, str) and dispatch_commit_ref:
                from portable_runtime.workflows.recovery_observation import (
                    RecoveryObservationCommitRequest,
                    reported_status_from_capability_status,
                )

                commit_observation = getattr(self.store, "commit_recovery_observation", None)
                try:
                    if not callable(commit_observation):
                        raise RuntimeError("StateStore lacks commit_recovery_observation")
                    observation = commit_observation(
                        RecoveryObservationCommitRequest(
                            observation_instance_ref=new_id("recovery_observation_instance"),
                            dispatch_commit_ref=dispatch_commit_ref,
                            observation_source="provider-reconcile",
                            reported_status=reported_status_from_capability_status(result.status),
                            provenance_refs=(last.provider_id,),
                        )
                    )
                    result = result.model_copy(
                        update={
                            "metadata": {
                                **result.metadata,
                                "recovery_observation_ref": observation.id,
                            }
                        }
                    )
                except Exception as exc:
                    step.status = "unknown"
                    self.store.save_step(step)  # type: ignore
                    return CapabilityResult(
                        request_id=last.request_ref,
                        provider_id=last.provider_id,
                        status="unknown",
                        message="reconciliation result could not be durably observed",
                        error={"code": "RecoveryObservationCommitFailed", "reason": str(exc)},
                    )
            if result.status == "unknown":
                step.status = "unknown"
                self.store.save_step(step)  # type: ignore
            return result
''',
          )

          test_path = Path("tests/conformance/test_recovery_observation_authority.py")
          test_text = test_path.read_text()
          for reason in (
              "B4-P1: durable RecoveryObservation is not implemented",
              "B4-P1: RecoveryObservation replay identity is not implemented",
              "B4-P1: observation instance identity is not implemented",
              "B4-P1: dispatch/action graph validation is not implemented",
              "B4-P1: direct RecoveryObservation event bypass is not closed",
              "B4-P1: same observation instance rebound protection is not implemented",
          ):
              marker = f'@pytest.mark.xfail(strict=True, reason="{reason}")\n'
              if marker not in test_text:
                  raise SystemExit(f"missing P0 xfail marker: {reason}")
              test_text = test_text.replace(marker, "", 1)
          test_text = test_text.replace(
              "from portable_runtime.core.models import Action, Event, Run, Step, StepAttempt, Work\n",
              "from portable_runtime.core.capabilities import CapabilityResult, ProviderDescriptor\n"
              "from portable_runtime.core.models import Action, Event, Run, Step, StepAttempt, Work\n"
              "from portable_runtime.core.registry import ProviderRegistry\n"
              "from portable_runtime.core.runtime import Runtime\n",
              1,
          )
          test_text += '''

          class _RuntimeReconcileProvider:
              descriptor = ProviderDescriptor(
                  id="provider:reconcile",
                  name="reconcile",
                  version="1",
                  capabilities=["deploy.apply"],
                  effect_semantics="reconcilable",
                  side_effect_class="reconcilable",
              )

              async def reconcile(self, request_id: str) -> CapabilityResult:
                  return CapabilityResult(
                      request_id=request_id,
                      provider_id=self.descriptor.id,
                      status="succeeded",
                  )


          @pytest.mark.asyncio
          @pytest.mark.parametrize("backend", ["memory", "sqlite"])
          async def test_b4_p1_007_runtime_reconcile_records_new_non_objective_observation(
              backend: str,
              tmp_path: Path,
          ) -> None:
              with _store(backend, tmp_path, f"runtime-reconcile-{backend}") as store:
                  graph = _seed_dispatch_graph(store, f"runtime-reconcile-{backend}")
                  registry = ProviderRegistry()
                  registry.register(_RuntimeReconcileProvider())  # type: ignore[arg-type]
                  runtime = Runtime(store=store, registry=registry)

                  first = await runtime.reconcile(graph["step_id"])
                  second = await runtime.reconcile(graph["step_id"])

                  assert first is not None and second is not None
                  assert first.status == "succeeded" and second.status == "succeeded"
                  assert first.metadata["recovery_observation_ref"] != second.metadata["recovery_observation_ref"]
                  observations = [
                      event
                      for event in store.list_events(graph["dispatch_ref"])
                      if event.type == "RecoveryObservationRecorded"
                  ]
                  assert len(observations) == 2
                  assert {event.payload["reported_status"] for event in observations} == {"reported-succeeded"}
                  assert all(event.payload["authoritative_outcome"] is False for event in observations)
                  assert store.list_records("Outcome") == []
                  assert store.get_work(graph["work_id"]).status != "completed"
                  assert store.get_run(graph["run_id"]).status != "succeeded"
          '''
          test_path.write_text(test_text)

          patch(
              "tests/conformance/test_recovery_terminal_closure_design.py",
              '''@_xfail("B4-A02: durable recovery observation authority is not implemented")
def test_b4_a02_reconciliation_must_be_durable_before_recovery_judgment() -> None:
    module = importlib.import_module("portable_runtime.workflows.recovery_closure")
    observation = module.RecoveryObservation(
        request_ref="request:b4",
        attempt_ref="attempt:b4",
        dispatch_commit_ref="dispatch:b4",
        source="provider-reconcile",
        result="succeeded",
    )
    assert observation.durable
    assert not observation.authoritative_outcome
    assert module.can_issue_recovery_disposition(observation_ref=None) is False
''',
              '''def test_b4_a02_reconciliation_must_be_durable_before_recovery_judgment() -> None:
    module = importlib.import_module("portable_runtime.workflows.recovery_observation")
    assert hasattr(module, "RecoveryObservation")
    assert hasattr(module, "RecoveryObservationCommitRequest")
    assert not hasattr(module, "RecoveryDisposition")
    assert not hasattr(module, "RecoveryApplication")
''',
          )

          Path("docs/f1-b4-recovery-observation.md").write_text('''# F1-B4 P1: durable RecoveryObservation

          ## Baselines and scope

          - F1-B3 implementation baseline: `25d834a174a5e884afcef532d3e5c3bd4f000107`.
          - F1-B4 design freeze: `d56f91749e4b8a86093ae07a14ff13ba4b43078e`.
          - This implementation closes B4-A02 only: durable observation of an already committed ambiguous dispatch.
          - B3-P4, terminal redesign, RecoveryDisposition, RecoveryApplication, compensation, and recovery portability remain out of scope.

          ## Authority contract

          ```text
          InvocationDispatchCommitted
              -> exact StepAttempt
              -> exact Step
              -> exact Action
              -> store-owned commit_recovery_observation()
              -> RecoveryObservationRecorded
          ```

          The caller supplies only an observation instance identity, dispatch commitment reference, observation source, execution-level report status, and provenance refs. The store re-reads the durable execution graph and derives Action / Attempt / Step / request / provider / idempotency bindings inside its transaction.

          `RecoveryObservation.reported_status` is deliberately one of `reported-succeeded`, `reported-failed`, or `reported-unknown`. It is not objective verification.

          ```text
          RecoveryObservation
              != confirmed Outcome
              != RecoveryDisposition
              != RecoveryApplication
              != provider invocation authority
              != terminal completion
          ```

          Observation identity is instance-based, not payload-deduplicated: exact instance replay is idempotent; a new instance with identical content is a new recovery fact; reusing one instance with different semantics is an identity rebound and fails closed.

          Live `Runtime.reconcile()` records an observation only for attempts carrying an E2b `dispatch_commit_ref`. Each actual provider reconciliation call receives a fresh observation instance. Observation persistence failure demotes the returned recovery result to `unknown` with `RecoveryObservationCommitFailed`.

          ## Explicit limits

          This phase does not define serialized RecoveryObservation portability. Bulk import/bundle authority closure is reserved for B4-P5. It also does not create any recovery policy, disposition, application, retry, compensation, governance discharge, or terminal authority.
          ''').write_text if False else None
          # The expression above keeps this YAML heredoc parser-safe; write the document normally below.
          doc = Path("docs/f1-b4-recovery-observation.md")
          doc.write_text(doc.read_text() if doc.exists() else "")
          doc.write_text("""# F1-B4 P1: durable RecoveryObservation

## Baselines and scope

- F1-B3 implementation baseline: `25d834a174a5e884afcef532d3e5c3bd4f000107`.
- F1-B4 design freeze: `d56f91749e4b8a86093ae07a14ff13ba4b43078e`.
- This implementation closes B4-A02 only: durable observation of an already committed ambiguous dispatch.
- B3-P4, terminal redesign, RecoveryDisposition, RecoveryApplication, compensation, and recovery portability remain out of scope.

## Authority contract

```text
InvocationDispatchCommitted
    -> exact StepAttempt
    -> exact Step
    -> exact Action
    -> store-owned commit_recovery_observation()
    -> RecoveryObservationRecorded
```

The caller supplies only an observation instance identity, dispatch commitment reference, observation source, execution-level report status, and provenance refs. The store re-reads the durable execution graph and derives Action / Attempt / Step / request / provider / idempotency bindings inside its transaction.

`RecoveryObservation.reported_status` is deliberately one of `reported-succeeded`, `reported-failed`, or `reported-unknown`. It is not objective verification.

```text
RecoveryObservation
    != confirmed Outcome
    != RecoveryDisposition
    != RecoveryApplication
    != provider invocation authority
    != terminal completion
```

Observation identity is instance-based, not payload-deduplicated: exact instance replay is idempotent; a new instance with identical content is a new recovery fact; reusing one instance with different semantics is an identity rebound and fails closed.

Live `Runtime.reconcile()` records an observation only for attempts carrying an E2b `dispatch_commit_ref`. Each actual provider reconciliation call receives a fresh observation instance. Observation persistence failure demotes the returned recovery result to `unknown` with `RecoveryObservationCommitFailed`.

## Explicit limits

This phase does not define serialized RecoveryObservation portability. Bulk import/bundle authority closure is reserved for B4-P5. It also does not create any recovery policy, disposition, application, retry, compensation, governance discharge, or terminal authority.
""")
          PY
          git config user.name "Xiongwei Lin"
          git config user.email "80614539+xiongweilin@users.noreply.github.com"
          git add src tests docs
          git commit -m "B4-P1: add durable recovery observation authority"
          git push origin HEAD:feat/recovery-observation-authority-impl
